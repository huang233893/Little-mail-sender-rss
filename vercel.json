{
  "version": 2, // 关键：声明用 V2 版本规则解析
  "crons": [
    {
      "path": "/api/cron",
      "schedule": "0 10 * * *"
    }
  ],
  "builds": [ // 关键：告诉 Vercel 如何运行你的代码（如果是 Node.js/TS 项目）
    { "src": "api/**/*.ts", "use": "@vercel/node" }
  ]
}
